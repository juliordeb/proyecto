<%= form_for(@stau) do |f| %>
  <% if @stau.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stau.errors.count, "error") %> prohibited this stau from being saved:</h2>

      <ul>
      <% @stau.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :contenido %><br>
    <%= f.text_field :contenido %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
